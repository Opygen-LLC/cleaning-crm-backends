//? User
enum UserRole {
    SUPER_ADMIN
    ADMIN
    STAFF
}

enum AccountStatus {
    PENDING
    ACTIVE
    SUSPENDED
    DELETED
}

enum Currency {
    USD
}

//? Subscription
enum SubscriptionName {
    STARTER
    GROWTH
    PRO
}

enum SubscriptionPlanInterval {
    MONTHLY
    YEARLY
}

enum SubscriptionStatus {
    ACTIVE
    CANCELLED
    EXPIRED
}

//? Coupon
enum DiscountType {
    PERCENTAGE // e.g. 20%
    FIXED // e.g. €10 off
}

//? Payment
enum PaymentMethod {
    STRIPE
}

enum PaymentStatus {
    PENDING
    PAID
    FAILED
    REFUNDED
    CANCELLED
}
